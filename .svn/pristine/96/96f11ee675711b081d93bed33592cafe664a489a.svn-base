using System.Data;

namespace Operational.Service
{
    public interface IArrivalService
    {
        DataTable GetArrival(string FromDate, string Todate, string ProductId, string ItemId, string SupId);
        DataTable GetArrival(string ARRIVAL_NO);
    }
}